{
  "name": "discord-bot-channel-reader",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace server && npm run build --workspace frontend",
    "start": "node server/dist/index.js"
  },
  "workspaces": [
    "server",
    "frontend"
  ]
}
